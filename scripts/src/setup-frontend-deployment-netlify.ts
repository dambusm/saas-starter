import { $ } from 'zx';

await $`echo "Go to https://app.netlify.com/start, connect to Github and select your repo. Ignore the setup form as it will be overwritten by the config files in the repo. This should be all you need to do."`;
